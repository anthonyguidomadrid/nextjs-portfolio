fragment ImageFields on UploadFile {
  alternativeText
  url
}

fragment SeoFields on ComponentHeadSeo {
  pageTitle
  pageDescription
  canonicalUrl
  noFollow
  noIndex
  shareImages {
    data {
      attributes {
        ...ImageFields
      }
    }
  }
}
